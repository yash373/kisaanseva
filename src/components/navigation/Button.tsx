import React from 'react'

interface ButtonProps {
    link: string,
    color: boolean
}

const Button = ({link , color}: ButtonProps) => {
  return (
    <div>
      button
    </div>
  )
}

export default Button
