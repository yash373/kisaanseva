import React from 'react'

interface InfoProps {
    img: string,
    title: string,
    para: string
}

const Info = ({img, title, para}: InfoProps) => {
  return (
    <div>
      
    </div>
  )
}

export default Info
